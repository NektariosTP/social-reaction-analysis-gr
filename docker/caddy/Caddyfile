:80 {
    @api path /health /events* /docs /openapi.json /redoc
    handle @api {
        reverse_proxy api:8000
    }

    handle {
        root * /srv
        encode gzip
        try_files {path} /index.html
        file_server
    }
}
